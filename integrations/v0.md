# Using Ramoira with v0

Use the schema as the brand context source for UI copy and marketing surfaces.

Recommended:

- resolve surface (e.g. `paid_landing_page`, `email_acquisition`)
- load only relevant schema sections (surface manifest)
- generate copy, then preflight check
